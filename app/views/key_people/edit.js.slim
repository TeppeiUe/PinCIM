| var setTime = 0;

- if action_name == "update"
	| var setTime = 500;
	| $("#key_person_edit-modal").modal('hide');

/modal('hide')のタイミングが遅いため、modal('hide')の実行時は0.5s後に実行(Chromeで確認、通信状態に依存するかは不明)
| setTimeout(function() {
| 	$("#key_person_edit-modal").html(
|       "#{j(render 'key_people/form', key_person: @key_person, btn_name: '変更保存')}"
|   );
|   $("#key_person_edit-modal").modal('show');
| 	$(document).on('shown.bs.modal',function(){
| 	 	$('#key_person_name').focus();
|		});
| }, setTime);